# Copy this file to ".env" and adjust values as needed.
# Loaded via pydantic-settings (uses python-dotenv) from `app/core/config.py`.

# Required for time series endpoint
POLYGON_API_KEY=
POLYGON_BASE_URL="https://api.polygon.io"


